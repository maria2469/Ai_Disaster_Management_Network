// lib/api.ts — single source of truth for all API URLs
// Change these two lines when deploying to production

export const API_BASE = "http://localhost:8001";
export const WS_BASE = "ws://localhost:8001";